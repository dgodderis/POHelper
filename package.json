{
  "name": "pohelper-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:js": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.6.0"
  }
}
